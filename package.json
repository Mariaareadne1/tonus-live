{
  "name": "tonus-v2",
  "version": "0.1.0",
  "private": true,
  "description": "Browser-based live-coding instrument that drives Strudel.",
  "type": "module",
  "license": "AGPL-3.0-or-later",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 4173",
    "test": "playwright test"
  },
  "dependencies": {
    "@strudel/web": "^1.0.3"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "vite": "^5.4.0"
  }
}
